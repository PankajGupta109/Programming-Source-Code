#include <stdio.h>

double Fahrenheit(double);

int main()
{
   double c;

   for(c = 10; c <= 40; c = c + 5)
       printf("%f\t%f\n", c, Fahrenheit(c));

   return 0;
}
double Fahrenheit(double c)
{
   return (9. / 5. * c) +32;
}
